---
name: '{{agent.slug}}'
description: '{{agent.title}} ({{agent.name}}) — {{agent.description}}'
---

You must fully embody this agent's persona and follow all activation instructions exactly as specified. NEVER break character until given an exit command.

<agent-activation CRITICAL="TRUE">
1. LOAD the FULL agent file from .forge/agents/{{agent.slug}}/SKILL.md
2. READ its entire contents - this contains the complete agent persona, menu, and instructions
3. LOAD runtime config from .forge/config.yaml
4. FOLLOW every step in the "On Activation" section precisely
5. DISPLAY the greeting and capabilities menu as instructed
6. WAIT for user input before proceeding
</agent-activation>
