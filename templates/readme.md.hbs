# {{config.project.name}}

{{#if config.project.description}}
{{config.project.description}}
{{/if}}

**Domain**: {{config.project.domain}}

## Quick Start

1. **Activate an agent** — run a launcher from your IDE skills:
   ```
   /wforge-agent-ingestion    # Activate Iris (Ingestion Agent)
   ```
   The agent presents a capabilities menu. Pick a code to run a skill.

2. **Ingest your first source**:
   ```
   /wforge-ingest-source      # Or use code IS from the Ingestion Agent menu
   ```
   Provide a PDF, URL, or file path. The skill converts, extracts, and updates the wiki.

3. **Search the wiki**:
   ```bash
   ./.forge/tools/search.sh "your query"
   ```

4. **Ask a question**:
   ```
   /wforge-query-wiki         # Or activate Quinn with /wforge-agent-query
   ```

5. **Navigate the system**:
   ```
   /wforge-help               # See available agents, skills, and next steps
   ```

## Directory Structure

```
{{config.project.name}}/
├── wiki/                          # LLM-maintained knowledge base
│   ├── index.md                   # Master page index
│   ├── log.md                     # Operation log (append-only)
│   ├── overview.md                # Project overview
│   ├── entities/                  # People, orgs, tools
│   ├── concepts/                  # Ideas, theories, frameworks
│   ├── sources/                   # Per-source summaries
│   └── comparisons/               # Analysis artifacts
├── raw/                           # Immutable source documents
├── outputs/                       # Generated deliverables
├── .forge/                        # WikiForge internals
│   ├── config.yaml                # Runtime configuration
│   ├── _config/                   # Manifests (catalogs)
│   ├── agents/                    # Agent personas (SKILL.md)
│   ├── skills/                    # Workflows by phase
│   ├── core/                      # Help & party mode
│   ├── templates/                 # Page templates
│   └── tools/                     # CLI helpers
└── .claude/skills/                # IDE launchers
```

## Available Agents

{{#each config.agents.enabled}}
{{#with (lookup @root.agentMeta this)}}
| {{this.icon}} | **{{this.name}}** | {{this.title}} | {{this.description}} |
{{/with}}
{{/each}}

## Research Lifecycle

| Phase | Skills | Purpose |
|-------|--------|---------|
| 1. Acquisition | ingest-source, batch-ingest, web-research | Bring sources into the wiki |
| 2. Compilation | update-entities, update-concepts, cross-reference | Build wiki pages |
| 3. Analysis | query-wiki, generate-comparison, adversarial-review | Extract insights |
| 4. Synthesis | compile-output, generate-slides, export-report | Create deliverables |
| Maintenance | lint-wiki, reindex, stats | Keep wiki healthy |

## Wiki Conventions

| Setting | Value |
|---------|-------|
| Citation format | {{config.wiki.citation_format}} |
| Cross-references | {{config.wiki.cross_ref_style}} |
| Organization | {{config.knowledge.organization}} |
| Scale | {{config.knowledge.scale}} |
| Source types | {{#each config.knowledge.source_types}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} |

---

*Generated by [WikiForge](https://github.com/wikiforge) on {{config.project.created}}.*
